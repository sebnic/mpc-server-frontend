{
  "name": "mcp-browser-poc",
  "version": "1.0.0",
  "description": "MCP Server running in browser with Web Worker",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.75",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "echarts": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vite": "^4.5.0"
  },
  "engines": {
    "node": ">=14.18.0"
  }
}
